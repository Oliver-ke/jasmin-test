const findMax = require('../functions/findMax');

describe('FindMax Test', function() {
	// it('Expect findMax to return maximum number', function() {
	// 	const res = findMax(14, 10);
	// 	expect(res).toBe(14);
	// });
	// it('Expect findMax to return false when numbers are equal', function() {
	// 	const res = findMax(3, 3);
	// 	expect(res).toBe(false);
	// });
	// it('Expect findMax to return error when a none number is passed', function() {
	// 	const res = findMax('shhfu', 'suurhew');
	// 	expect(res).toContain('error');
	// });
});
